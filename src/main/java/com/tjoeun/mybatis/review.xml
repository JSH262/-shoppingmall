<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Review">

	<insert id="insert" parameterType="ReviewVO">		
		insert into review(user_id, product_id, score, contents)
		values(#{userId}, #{productId}, #{score}, #{contents})
	</insert>
	
	<update id="update" parameterType="ReviewVO">
		update cart
		set	amount = nvl(amount, 0) + #{amount}
		where user_id = #{userId} and product_id = #{productId}
	</update>
		
	<delete id="delete" parameterType="ReviewVO">
		delete 
		from cart
		where
			<if test="userId != null"> 
				user_id = #{userId}
			</if>
			<if test="productId != null"> 
				and product_id = #{productId}
			</if>  
			
	</delete>
	
	
	<select id="productIds" parameterType="ReviewVO" resultType="ReviewVO">
		select product_id
		from cart
		where user_id = #{userId}
	</select>
	
	
</mapper>




