<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Review">

	<insert id="insert" parameterType="ReviewVO">		
		insert into review(id, user_id, product_id, score, contents)
		values(review_seq_id.nextval, #{userId}, #{productId}, #{score}, #{contents})
	</insert>
		
	<update id="delete" parameterType="ReviewVO">
		update review
		set	use_yn = "N"
		where id = #{Id}
	</update>
	
	
	<select id="reviewIds" parameterType="ReviewVO" resultType="ReviewVO">
		select id
		from review
		where user_id = #{userId}
	</select>
	
	
</mapper>




