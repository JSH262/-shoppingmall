<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Category">

	<insert id="insert" parameterType="CategoryVO">
		insert into category (id, name, gup, lev, seq, type) 
		values (category_seq_id.nextval, #{name}, category_seq_id.currval, 0, 0, #{type})
	</insert>

	<select id="selectList" resultType="CategoryVO">
		select *
		from category
		order by gup desc, seq asc
	</select>
	
	<update id="increment" parameterType="java.util.HashMap">
		update category set seq = seq + 1 where gup = #{gup} and seq >= #{seq}
	</update>
	
	<insert id="reply" parameterType="CategoryVO">
		insert into category (id, name, gup, lev, seq) 
		values (category_seq_id.nextval, #{name}, #{gup}, #{lev}, #{seq})
	</insert>
	
	<delete id="delete" parameterType="int">
		delete from category where id = #{id}
	</delete>
	
	<select id="selectByid" parameterType="int" resultType="CategoryVO">
		select * from category where id = #{id}
	</select>
	
	<update id="deleteCategory" parameterType="int">
		update category set category = '삭제된 카테고리 입니다.' where id = #{id}
	</update>
	
	<update id="useYn" parameterType="int">
		update category set use_yn = #{useYn} where id = #{id}
	</update>
	
	<update id="update" parameterType="CategoryVO">
		update category set name = #{name} where id = #{id}
	</update>
	
	<select id="deleteList" parameterType="CategoryVO" resultType="CategoryVO">
		select * from category where gup = #{gup} and lev > #{lev} and seq > #{seq} or id = #{id} 
		order by gup desc, seq asc
	</select>
	
	<select id="selectGup" parameterType="int" resultType="CategoryVO">
		select * from category where gup = #{gup} order by gup desc, seq asc
	</select>
	
	<update id="reSeq" parameterType="java.util.HashMap">
		update category set seq = #{i} where id = #{id}
	</update>
	
	<update id="modifyDate" parameterType="CategoryVO">
		update category set modify_Date = sysdate where id = #{id}
	</update>
	
</mapper>














